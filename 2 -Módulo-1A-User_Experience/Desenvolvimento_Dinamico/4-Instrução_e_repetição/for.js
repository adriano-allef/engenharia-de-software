// Imagine que você é um entregador de pizzar em uma cidade com várias ruas de 1 a 10.
// Você precisa entregar as pizzas em cada rua, começando da rua 1 até a rua 10.

// Aqui usaremos um loop "for" para simular essa entrega.

for (let rua = 1; rua <= 10; rua++) {
    console.log(`Entregando pizza na rua ${rua}`);

}